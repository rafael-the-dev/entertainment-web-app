import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(theme => ({
    cardContentTrending: {
        backgroundImage: 'linear-gradient(to bottom, rgba(0, 0, 0, .2), rgba(0, 0, 0, .5))'
    }
}));